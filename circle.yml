machine:
  python:
      version: 3.5.0
test:
  override:
    - make html
deployment:
  release:
    branch: master
    commands:
      - git commit -m "update gh-pages by circleci"
      - git subtree push --prefix output origin gh-pages
